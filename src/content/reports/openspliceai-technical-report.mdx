---
title: 'OpenSpliceAI: an efficient, modular PyTorch framework for splice-site prediction, and a genome-wide variant re-scoring campaign'
description: 'A technical report on OpenSpliceAI — a PyTorch reimplementation and extension of SpliceAI — covering its architecture and end-to-end pipeline, a head-to-head accuracy and speed benchmark against the original SpliceAI on the human MANE test set, a variant delta-score comparison, and a genome-wide campaign that re-scores every human GRCh38 SNV.'
authors:
  - Kuan-Hao Chao
abstract: >-
  SpliceAI set the standard for deep-learning splice-site prediction, but its original implementation is locked to a
  TensorFlow/Keras runtime, ships as fixed human weights with no supported path to retrain on other species, and exposes
  only a variant-scoring entry point — not the data-preparation, training, calibration, and genome-scale prediction
  pipeline behind it. OpenSpliceAI is a ground-up reimplementation in PyTorch that reproduces the SpliceAI residual
  dilated-CNN architecture exactly while exposing the full lifecycle as a single console tool with six composable
  subcommands: create-data, train, transfer, calibrate, predict, and variant. The same model class is keyed on one
  central parameter — the flanking (context) size, with the identity CL = 2·Σ AR(W−1) tying the dilation schedule to a
  context length of 80, 400, 2,000, or 10,000 nt — and is reused across every subcommand, so a dataset, a checkpoint,
  and an inference run are always consistent. On the human MANE test set (420.58 M scored positions, 5-model ensembles
  at 10,000 nt), OpenSpliceAI matches or exceeds the original SpliceAI on every top-kL accuracy threshold, on AUPRC, on
  precision, and on F1 for both donors and acceptors (e.g. donor Top-1L 0.944 vs 0.919, AUPRC 0.981 vs 0.966), trading a
  little recall for markedly higher precision, and it is 1.47× faster on the same hardware. A batched, reference-
  deduplicated variant scorer makes whole-genome variant effect prediction tractable — about 6.7× faster than the
  original per-variant loop. We use that engine to launch a genome-wide campaign that writes an OpenSpliceAI delta-score
  field next to SpliceAI's existing field at every human GRCh38 SNV (~3.43 billion SNVs per model seed), enabling the
  first strictly mask-matched, tool-neutral comparison of the two methods. The campaign is a managed, good-citizen
  SLURM workload — idempotent at chunk granularity, serialized across model seeds into a dependency chain so it never
  exceeds a self-imposed GPU cap on a shared ten-GPU account. That campaign has now scored 99,283 of 100,000 chunks for
  one seed and 45,597 for a second, enough for the mask-matched comparison it was built for: over 3.33 billion paired
  annotations the two tools agree moderately overall (Pearson 0.795), but the agreement is strongly asymmetric — loss
  scores agree closely (0.900 and 0.906) while gain scores do not (0.681 and 0.597). Scoring a second independently
  seeded checkpoint supplies the control that makes this interpretable: on losses the gap between the two tools is
  barely larger than the gap between two training runs of one tool (1.1×), while on gains it is 3.3–3.9×, localising
  the difference to gain-of-site prediction rather than to training-run variance. This report documents the
  architecture, the benchmarks, and the campaign design, status and results.
date: 2026-06-18
updated: 2026-09-08
venue: 'Technical Report'
institution: 'Johns Hopkins University'
tags:
  - Splice prediction
  - Deep learning
  - Variant effect prediction
  - Genomics
  - Open source
image: ../../assets/reports/openspliceai-technical-report/rfig_accuracy.png
imageAlt: 'Per-position accuracy of OpenSpliceAI versus the original SpliceAI on the human MANE test set; OpenSpliceAI is at or above SpliceAI on every threshold-accuracy, AUPRC, precision, and F1 metric for both donors and acceptors.'
scholar: true
unlisted: true
references:
  - text: "Chao K-H, Mao A, Liu A, Pertea M, Salzberg SL. OpenSpliceAI provides an efficient modular implementation of SpliceAI enabling easy retraining across nonhuman species. eLife 14:RP107454, 2025."
    doi: https://doi.org/10.7554/eLife.107454.3
  - text: "Jaganathan K, Kyriazopoulou Panagiotopoulou S, McRae JF, Darbandi SF, Knowles D, Li YI, Kosmicki JA, et al. Predicting splicing from primary sequence with deep learning. Cell 176(3):535–548, 2019."
    doi: https://doi.org/10.1016/j.cell.2018.12.015
  - text: "Paszke A, Gross S, Massa F, et al. PyTorch: an imperative style, high-performance deep learning library. Advances in Neural Information Processing Systems 32, 2019."
    url: https://papers.nips.cc/paper/9015-pytorch-an-imperative-style-high-performance-deep-learning-library
  - text: "Loshchilov I, Hutter F. Decoupled weight decay regularization (AdamW). International Conference on Learning Representations, 2019."
    url: https://arxiv.org/abs/1711.05101
  - text: "Lin T-Y, Goyal P, Girshick R, He K, Dollár P. Focal loss for dense object detection. IEEE International Conference on Computer Vision (ICCV), 2017."
    doi: https://doi.org/10.1109/ICCV.2017.324
  - text: "Guo C, Pleiss G, Sun Y, Weinberger KQ. On calibration of modern neural networks. International Conference on Machine Learning (ICML), 2017."
    url: https://arxiv.org/abs/1706.04599
  - text: "Morales J, Pujar S, Loveland JE, et al. A joint NCBI and EMBL-EBI transcript set for clinical genomics and research (MANE). Nature 604:310–315, 2022."
    doi: https://doi.org/10.1038/s41586-022-04558-8
  - text: "Shirley MD, Ma Z, Pedersen BS, Wheelan SJ. Efficient pythonic access to FASTA files using pyfaidx. PeerJ Preprints 3:e1196, 2015."
    doi: https://doi.org/10.7287/peerj.preprints.970v1
  - text: "Dale RK, Pedersen BS, Quinlan AR. gffutils: Python tools for handling GFF/GTF annotation files."
    url: https://github.com/daler/gffutils
  - text: "Yoo AB, Jette MA, Grondona M. SLURM: Simple Linux Utility for Resource Management. Job Scheduling Strategies for Parallel Processing (JSSPP), 2003."
    doi: https://doi.org/10.1007/10968987_3
---
import ZoomFigure from '../../components/ZoomFigure.astro';
import architecture from '../../assets/reports/openspliceai-technical-report/rfig_architecture.png';
import accuracy from '../../assets/reports/openspliceai-technical-report/rfig_accuracy.png';
import speed from '../../assets/reports/openspliceai-technical-report/rfig_speed.png';
import campaignDesign from '../../assets/reports/openspliceai-technical-report/rfig_campaign_design.png';
import campaignProgress from '../../assets/reports/openspliceai-technical-report/rfig_campaign_progress.png';
import calibration from '../../assets/reports/openspliceai-technical-report/rfig_calibration.png';
import seedVsMethod from '../../assets/reports/openspliceai-technical-report/rfig_seed_vs_method.png';

*Center for Computational Biology, Johns Hopkins University, Baltimore, MD, USA. Correspondence: kh.chao@jhu.edu. Code: [github.com/Kuanhao-Chao/OpenSpliceAI](https://github.com/Kuanhao-Chao/OpenSpliceAI) · Documentation: [ccb.jhu.edu/openspliceai](https://ccb.jhu.edu/openspliceai/). This report describes OpenSpliceAI v0.0.7. Sections 3.3, 4.4 and 4.5 were revised on 2026-09-08 with the completed genome-wide comparison; other numbers are current as of the date above.*

## 1. Introduction

### 1.1 Splice-site prediction and SpliceAI

Pre-mRNA splicing removes introns and joins exons, and the choice of splice sites — the donor (5′) and acceptor (3′) boundaries — determines the protein a gene encodes. Predicting those boundaries from primary sequence alone is a foundational genomics problem with direct clinical stakes: a large fraction of disease-causing variants act by creating or destroying splice sites, often far from the canonical dinucleotides. SpliceAI [2] established the state of the art by framing the task as dense, per-nucleotide three-way classification — every position is *not-a-site*, *acceptor*, or *donor* — solved with a deep residual dilated 1-D convolutional network that reads up to 10,000 nt of flanking context. Its variant module, which scores how a substitution shifts predicted splicing, became a standard component of clinical variant-interpretation pipelines.

### 1.2 Why a reimplementation

SpliceAI is influential but, as a software artifact, closed in several practical ways:

1. **Runtime lock-in.** The released model is TensorFlow/Keras weights. Modern training, profiling, and deployment tooling has consolidated around PyTorch [3], and the original code is not maintained against it.
2. **No supported retraining.** The published weights are human-only, and there is no first-class, documented path to retrain the architecture on a new species, a new annotation, or a different context size — exactly what comparative and non-model-organism genomics needs.
3. **A pipeline reduced to one entry point.** Variant scoring is exposed, but the steps that produce a model — building one-hot windowed training data from a genome and annotation, training, calibrating probabilities, and running genome-scale prediction — are not packaged as a usable, composable workflow.
4. **Scaling friction.** The original variant scorer processes one variant at a time, which is adequate for a clinical VCF but far too slow for genome-wide studies that score billions of SNVs.

### 1.3 What OpenSpliceAI adds

OpenSpliceAI [1] is a from-scratch PyTorch reimplementation and extension that keeps the SpliceAI architecture intact while opening the entire lifecycle. It is distributed as a single `openspliceai` console command with six composable subcommands forming an end-to-end pipeline — **create-data → train / transfer → calibrate → predict / variant** (Fig. 1B). The same `SpliceAI` network class is built by every subcommand, parameterized by one central knob (the flanking/context size), so datasets, checkpoints, and inference runs are guaranteed mutually consistent. On top of architectural parity it adds: documented **training and transfer learning** (including layer-freezing fine-tuning across species), **probability calibration**, **genome-scale prediction** with automatic long-sequence tiling, a **batched, reference-deduplicated variant scorer** for whole-genome throughput, a **model zoo** of five-seed ensembles for five species at four context sizes, and a tested, lint-clean codebase.

### 1.4 Contributions

This report documents OpenSpliceAI and validates it against the original SpliceAI on real data. The principal points are:

- A faithful PyTorch reimplementation of the SpliceAI residual dilated-CNN whose context length is tied to the dilation schedule by the exact identity $CL = 2\sum_i AR_i(W_i-1)$, reused across all six subcommands (§2).
- A head-to-head benchmark on the human MANE test set showing OpenSpliceAI **at or above** the original SpliceAI on every top-kL accuracy threshold, AUPRC, precision, and F1 for both donors and acceptors, while being **1.47× faster** on identical hardware (§3.1–3.2).
- A batched, reference-deduplicated variant-scoring engine (~**6.7×** over the per-variant loop) that makes genome-wide variant effect prediction tractable (§2.7, §3.2).
- A genome-wide campaign that re-scores **every human GRCh38 SNV** with OpenSpliceAI alongside SpliceAI's existing scores — a strictly mask-matched, tool-neutral overlay — engineered as an idempotent, GPU-capped, good-citizen SLURM workload, reported here with its current live status (§4).

## 2. Architecture and pipeline

### 2.1 The model

OpenSpliceAI defines one network, the `SpliceAI` module, and reuses it everywhere (Fig. 1A). It is a deep residual, dilated 1-D CNN that consumes **4-channel one-hot DNA** (A, C, G, T) and emits, for each output position, a **3-class softmax** over three classes — not-a-site, acceptor, and donor. Its building blocks are:

- **ResidualUnit** — a pre-activation block applying BatchNorm → LeakyReLU(0.1) → Conv1d **twice**, with symmetric padding $(W-1)\cdot AR/2$ that preserves length, then a residual add of the block's input. Convolution window $W$ and dilation (atrous) rate $AR$ are the two per-unit hyperparameters.
- **Skip connections** — a $1\times1$ convolution taps the representation after the input stem and after **every four** residual units, and these taps are summed into an accumulator. This is the mechanism by which shallow and deep features are combined before the head.
- **Cropping1D** — the accumulated signal is trimmed by $CL/2$ positions on each end, converting a window of length $SL + CL$ down to the $SL$ central positions that actually receive predictions.
- **Output head** — a final $1\times1$ convolution to 3 channels followed by softmax over the class dimension.

Two global constants fix the geometry (`openspliceai/constants.py`): the maximum context $CL_{max} = 10{,}000$ (padded onto both ends) and the prediction window $SL = 5{,}000$. Every input window is therefore $SL + CL_{max} = 15{,}000$ positions wide; the model is fed tensors of shape $(N, 4, 15000)$ and returns $(N, 3, 5000)$.

### 2.2 Flanking size: the one central parameter

The single most important parameter is the **flanking (context) size**, valid only in $\{80, 400, 2000, 10000\}$. It selects a fixed schedule of residual units and their $(W, AR)$ values (Table 2). The schedule is constructed so that the **receptive-field context length equals the flanking size exactly**, through the identity

$$CL = 2\sum_{i} AR_i\,(W_i - 1).$$

For example, the 10,000-nt model stacks 16 residual units in four dilation tiers, and $2\,[\,4\cdot1\cdot10 + 4\cdot4\cdot10 + 4\cdot10\cdot20 + 4\cdot25\cdot40\,] = 10{,}000$. Because the flanking size must agree between the dataset, the model definition, and the checkpoint, a mismatch surfaces immediately as a tensor-shape error rather than as silently wrong predictions.

**Table 1.** The six subcommands and the end-to-end pipeline.

| # | Subcommand | Role |
|---|---|---|
| 1 | `create-data` | Build one-hot, windowed training tensors from a genome FASTA + GFF annotation (two stages: `create_datafile` → `create_dataset`). |
| 2 | `train` | Train a `SpliceAI` model from scratch at a chosen flanking size. |
| 3 | `transfer` | Fine-tune a pretrained checkpoint on a new dataset/species, optionally freezing all but the last *k* residual units. |
| 4 | `calibrate` | Post-hoc temperature scaling of a trained model's probabilities. |
| 5 | `predict` | Score a FASTA (optionally just gene regions) and emit donor/acceptor BED tracks; ensembles a directory of checkpoints. |
| 6 | `variant` | Annotate a VCF with splicing delta scores (PyTorch or Keras backend). |

**Table 2.** Hyperparameter schedule keyed on flanking size (channels $L = 32$; batch size scales as a base × `N_GPUS` = 2).

| Flanking (= CL) | Residual units | $W$ (conv window) | $AR$ (dilation) | Batch size |
|---:|---:|---|---|---:|
| 80 | 4 | 11 × 4 | 1 × 4 | 36 |
| 400 | 8 | 11 × 8 | 1 × 4, 4 × 4 | 36 |
| 2,000 | 12 | 11 × 8, 21 × 4 | 1 × 4, 4 × 4, 10 × 4 | 24 |
| 10,000 | 16 | 11 × 8, 21 × 4, 41 × 4 | 1 × 4, 4 × 4, 10 × 4, 25 × 4 | 12 |

<ZoomFigure src={architecture} alt="Two-panel schematic: (A) the SpliceAI residual dilated 1-D CNN from one-hot input through residual units with skip connections, cropping, and a 3-channel softmax head; (B) the six-subcommand pipeline create-data, train/transfer, calibrate, predict, variant" variant="wide">
  <strong>Figure 1.</strong> OpenSpliceAI's model and pipeline. <strong>(A)</strong> The `SpliceAI` network: 4-channel one-hot DNA of width $SL+CL_{max}=15{,}000$ → Conv1d stem → four groups of (ResidualUnit × 4) with a $1\times1$ Skip tap after the stem and each group → Cropping1D trims to the $SL=5{,}000$ central positions → $1\times1$ conv + softmax over 3 classes (not-a-site / acceptor / donor). The dilation schedule satisfies $CL = 2\sum AR_i(W_i-1)$ = the flanking size. <strong>(B)</strong> The same model class flows through all six subcommands; checkpoints are plain `state_dict`s and a directory of seeds is ensembled by averaging.
</ZoomFigure>

### 2.3 Data representation (`create-data`)

`create-data` runs two stages in one invocation. **Stage 1** (`create_datafile`) walks the annotation with `gffutils`, extracts each gene's sequence from the FASTA, **reverse-complements genes on the minus strand**, and marks splice labels (donor = 2, acceptor = 1, else 0), optionally gating sites to canonical motif pairs (GT-AG / GC-AG / AT-AC) under `--canonical-only`. The result is a per-gene HDF5 (`datafile_*.h5`) holding string datasets `SEQ`, `LABEL`, `STRAND`, `TX_START`, `TX_END`. **Stage 2** (`create_dataset`) one-hot encodes those genes into fixed-length windows and writes chunked tensor datasets `X0, X1, …` and `Y0, Y1, …` (100 genes per chunk) in `dataset_*.h5`. Encoding uses two fixed maps: nucleotides A, C, G, T → 1, 2, 3, 4 and N/padding → 0 (`IN_MAP`); labels 0, 1, 2 (`OUT_MAP`). Training reads these shards and transposes them to channels-first $(N, 4, L)$ before the model.

Chromosome assignment follows one of two policies. `--split-method human` reproduces SpliceAI's fixed train/test partition (test = chr1, 3, 5, 7, 9); `random` splits by cumulative length to a target `--split-ratio`. The training portion is then split 90:10 into train/validation.

### 2.4 Training and transfer

`train` and `transfer` build the model, then share one `train_model` loop (`openspliceai/train_base/`). Data is streamed shard-by-shard from HDF5, and every batch is cropped to the model's required context before the forward pass. The loss is either a two-dimensional categorical cross-entropy (default) or a focal loss [5] ($\gamma = 2$) for class imbalance, selectable with `--loss`. The optimizer is **AdamW** [4] (learning rate 1e-3 for training, 1e-4 for transfer), with either a `MultiStepLR` schedule (×0.5 over the final epochs) or `CosineAnnealingWarmRestarts` ($T_0 = 5$). Checkpoints are saved as **plain `state_dict`s** (`model_{epoch}.pt`, `model_best.pt`). Per-epoch metrics — top-k accuracy and AUPRC for donor and acceptor, plus per-class precision/recall/F1 — are appended to log files under a structured output tree.

`transfer` additionally loads a pretrained checkpoint, filtering out size-mismatched keys, and can **freeze all but the last `--unfreeze` residual units** so that fine-tuning on a new species adapts only the head. This is the mechanism behind OpenSpliceAI's cross-species results in the published study [1].

> **A note on `N_GPUS`.** The hyperparameter tables hardcode `N_GPUS = 2`, but the code does **not** wrap the model in `DataParallel`; it runs on a single device chosen by preference order CUDA → MPS → CPU. `N_GPUS` only scales the batch size and the remainder-trimming in batch clipping. This is a known quirk, not multi-GPU training.

### 2.5 Genome-scale prediction (`predict`)

`predict` is staged to scale to whole genomes. It optionally restricts to gene regions when given an annotation (`-a`), splits any FASTA entry longer than `--split-threshold` (default 1.5 Mb) into tiles **with flanking overlap so that predictions are seamless across tile boundaries**, one-hot encodes, runs the model, and writes `donor_predictions.bed` and `acceptor_predictions.bed`. There are two modes: the default **turbo** path streams predictions straight to BED with no intermediate file, while `--predict-all` materializes a `predict.h5` first. **If `--model` is a directory, every checkpoint in it is ensembled by averaging predictions** — the intended way to use the five-seed model zoo.

### 2.6 Variant effect scoring (`variant`)

`variant` annotates a VCF with splicing delta scores. An `Annotator` loads the reference genome (`pyfaidx`), a gene-annotation table (built-in `grch37`/`grch38`, now packaged with the tool, or a custom TSV), and one or more models (PyTorch by default, or the original Keras weights with `--model-type keras`). For each variant, within `--distance` (default 50) of each overlapping gene, it constructs reference and alternate one-hot windows (reverse-complemented on the minus strand), predicts both, and computes four **delta scores** as the maximum per-position change in the relevant class probability:

$$
\begin{aligned}
DS_{AG} &= \max\big(y^{alt}_{\text{acc}} - y^{ref}_{\text{acc}}\big), &
DS_{AL} &= \max\big(y^{ref}_{\text{acc}} - y^{alt}_{\text{acc}}\big), \\
DS_{DG} &= \max\big(y^{alt}_{\text{don}} - y^{ref}_{\text{don}}\big), &
DS_{DL} &= \max\big(y^{ref}_{\text{don}} - y^{alt}_{\text{don}}\big),
\end{aligned}
$$

for acceptor/donor gain/loss, together with their argmax **positions** $DP_{*}$. Results are written to an `OpenSpliceAI` INFO field with the format `ALLELE|SYMBOL|DS_AG|DS_AL|DS_DG|DS_DL|DP_AG|DP_AL|DP_DG|DP_DL`. The `-M/--mask` flag zeroes scores that contradict the annotation (gains at already-annotated sites; losses away from annotated sites), matching the convention of SpliceAI's *masked* precompute.

### 2.7 The batched variant scorer

The original variant path scores one window at a time — fine for a clinical VCF, far too slow for billions of SNVs. OpenSpliceAI adds `get_delta_scores_batched` (`openspliceai/variant/utils.py`), enabled by `-b/--batch-size > 1` on the PyTorch backend, with **identical per-variant math** to the scalar path but two throughput wins:

1. **Within-position reference deduplication.** The reference window at a locus depends only on (chromosome, position, gene) — *not* on the alternate allele — so the (typically three) alternate alleles at a site share a single reference forward pass. The batch is assembled as `[unique refs][all alts]`, removing the redundant reference passes (≈ 1.3–1.5× fewer windows).
2. **Batched inference.** All collected windows are run through the ensemble in sub-batches of `batch_size` under `torch.inference_mode()`, with the ensemble prediction taken as the mean across seed models. Two env-tunable knobs accelerate this on A100-class GPUs: cuDNN autotuning (`OSAI_CUDNN_BENCH`, on by default, beneficial because all windows share one length) and TF32 matmuls (`OSAI_TF32`, on by default, ~2× with ~1e-3 numeric noise that is far below the reported score resolution; set to 0 for bit-reproducible fp32).

In aggregate this is about **6.7× faster** than the per-variant loop (Fig. 3B), and it is the engine that makes the genome-wide campaign of §4 feasible.

### 2.8 Calibration (`calibrate`)

Softmax outputs are not guaranteed to be well-calibrated probabilities. `calibrate` applies post-hoc **temperature scaling** [6]: a single scalar temperature $\tau$ is fit on the validation set by minimizing NLL, and predictions become $\mathrm{softmax}(\text{logits}/\tau)$. It reports Expected Calibration Error, NLL, and Brier scores before and after, writes calibration-curve plots, and saves `temperature.pt`/`.txt` plus a full `calibrated_model.pt`.

## 3. Results

### 3.1 Per-position accuracy vs. the original SpliceAI

We benchmarked OpenSpliceAI against the original SpliceAI on the **human MANE test set** [7] — the paralog-removed test split following SpliceAI's own protocol — streaming the full test set (no sub-sampling) through both **five-model ensembles at 10,000 nt** and computing the canonical SpliceAI metrics. Both backends scored the same **420.58 million positions** (53,849 true donors and 53,849 true acceptors), with channel order fixed identically. OpenSpliceAI is **at or above** SpliceAI on every threshold accuracy (Top-0.5L/1L/2L/4L), on AUPRC, on precision, and on F1, for **both** donors and acceptors (Table 3, Fig. 2). The clearest gaps are at the operating point that matters most — Top-1L accuracy (donor +0.0245, acceptor +0.0254) and AUPRC (donor +0.0151, acceptor +0.0139) — and in precision (+0.11 each), which lifts F1 by ~0.05 despite a small recall give-back.

**Table 3.** Per-position accuracy on the human MANE test set (10,000 nt, 5-model ensembles, 420.58 M positions; $n_{\text{true}} = 53{,}849$ for each class). Δ = OpenSpliceAI − SpliceAI.

| Metric | Donor OSAI | Donor SpliceAI | Δ | Acceptor OSAI | Acceptor SpliceAI | Δ |
|---|---:|---:|---:|---:|---:|---:|
| Top-0.5L accuracy | **0.9955** | 0.9884 | +0.0071 | **0.9964** | 0.9920 | +0.0045 |
| Top-1L accuracy | **0.9438** | 0.9193 | +0.0245 | **0.9469** | 0.9215 | +0.0254 |
| Top-2L accuracy | **0.9952** | 0.9927 | +0.0025 | **0.9939** | 0.9907 | +0.0032 |
| Top-4L accuracy | **0.9985** | 0.9977 | +0.0008 | **0.9977** | 0.9963 | +0.0014 |
| AUPRC | **0.9813** | 0.9662 | +0.0151 | **0.9825** | 0.9686 | +0.0139 |
| Precision | **0.9486** | 0.8357 | +0.1129 | **0.9547** | 0.8479 | +0.1068 |
| Recall | 0.9383 | **0.9614** | −0.0232 | 0.9393 | **0.9575** | −0.0182 |
| F1 | **0.9434** | 0.8942 | +0.0492 | **0.9469** | 0.8994 | +0.0475 |

The recall/precision trade is worth stating plainly: OpenSpliceAI calls splice sites **more conservatively** than SpliceAI — slightly lower recall, substantially higher precision — which nets out to higher F1 and AUPRC. §3.3 shows the variant-level counterpart is more specific than that summary suggests: OpenSpliceAI is the more conservative caller at permissive delta-score cutoffs but not at stringent ones, and the difference is concentrated in gain-of-site prediction. These are the *retrained* PyTorch models, which differ from the original weights; at flanking 10,000 the architectures are equivalent enough that the Keras weights can be loaded and reproduced bit-exactly through the `variant --model-type keras` path, but for accuracy the PyTorch zoo is a genuinely different, retrained model (with the largest gains, per the published study, appearing on non-human species [1]).

<ZoomFigure src={accuracy} alt="Grouped bar charts comparing OpenSpliceAI and SpliceAI on donor and acceptor metrics; OpenSpliceAI is at or above SpliceAI on every threshold accuracy, AUPRC, and F1" variant="wide">
  <strong>Figure 2.</strong> Per-position accuracy on the human MANE test set (10,000 nt, 5-model ensembles), donors <strong>(A)</strong> and acceptors <strong>(B)</strong>. OpenSpliceAI (green) is at or above the original SpliceAI (grey) on every threshold accuracy, on AUPRC, and on F1; the y-axis starts at 0.84 to make the gaps visible.
</ZoomFigure>

### 3.2 Inference speed

On the same benchmark run and the same GPU, scoring all 420.58 M positions took OpenSpliceAI **1,491 s** versus SpliceAI's **2,185 s** — a **1.47×** speedup from the PyTorch runtime alone (Fig. 3A). The larger lever is the batched variant scorer of §2.7: on the genome-wide workload it is about **6.7× faster** than the original per-variant loop (Fig. 3B), running at roughly **122 s per 34,334-SNV chunk** on a shared A100. That factor is the difference between a genome-wide SNV study being a multi-year proposition and a managed multi-month one.

<ZoomFigure src={speed} alt="Two-panel bar chart: (A) whole-test-set scoring wall-clock 1491 s for OpenSpliceAI vs 2185 s for SpliceAI; (B) variant delta-score engine relative throughput, batched -b 128 is 6.7x the per-variant baseline" variant="wide">
  <strong>Figure 3.</strong> Inference speed. <strong>(A)</strong> Whole-test-set scoring (420.58 M positions, same GPU): OpenSpliceAI is 1.47× faster than the Keras SpliceAI. <strong>(B)</strong> The batched, reference-deduplicated variant scorer (`-b 128`) reaches ~6.7× the throughput of the original per-variant loop, ~122 s per chunk on a shared A100.
</ZoomFigure>

### 3.3 Variant delta-score comparison

Beyond per-position accuracy, the clinically relevant question is whether the two tools *agree on the effect of a variant*. The campaign in §4 makes that question answerable at genome scale and, crucially, under matched conditions: **3,334,708,099 exact-gene paired annotations** across 99,283 audited chunks, with both sides masked (`-M 1`), scored against the same GENCODE v24 canonical gene table, at the same maximum distance and flanking context. This supersedes an earlier 452,053-variant comparison in which OpenSpliceAI was scored *unmasked* against SpliceAI's masked precompute — a confound that acted precisely on the gain side and made the central asymmetry unattributable.

Agreement on the maximum delta score is **moderate**: Pearson 0.795, Lin concordance 0.790 (Table 4). At a 0.5 cutoff, overall agreement is 99.90%, but that number is an artifact of the shared negative mass — 99.79% of pairs are below the cutoff in both tools — and should not be quoted; the chance-corrected view is $\kappa = 0.703$ with a positive-class Jaccard of 0.542, meaning that of the variants called by at least one tool, only about half are called by both.

The disagreement is **strongly asymmetric between losses and gains**, and that asymmetry survives the mask-matched control. Acceptor and donor *loss* scores agree closely (Pearson 0.900 and 0.906; identical values for 99.06% of variants), while *gain* scores do not (0.681 and 0.597), with OpenSpliceAI scoring gains systematically lower. Figure 4 shows the mechanism directly: for a variant SpliceAI scores as a moderate gain, OpenSpliceAI's median score stays near zero until SpliceAI's own score is well above 0.5, whereas for losses the two track the diagonal.

Two further properties matter in practice. **Output precision is not the explanation**: rounding OpenSpliceAI to SpliceAI's two decimals raises exact agreement from 82.11% to 93.27% but changes mean absolute error by 3 × 10⁻⁶, and 88.6% of the mean absolute difference survives discarding every discrepancy inside the ±0.005 rounding band. And **call behaviour crosses over with the threshold**: OpenSpliceAI makes only 0.44× as many calls as SpliceAI at a 0.1 cutoff but 1.06× as many at 0.8, so "OpenSpliceAI is the more conservative caller" holds only at permissive cutoffs. Agreement improves monotonically with the cutoff ($\kappa$ 0.445 → 0.779), which is to say the two tools converge exactly where decisions are made. Where both call a variant at 0.5, they agree on the affected position essentially perfectly for losses (100.00%) and nearly so for gains (96.8% and 98.1%).

**Table 4.** Genome-wide mask-matched delta-score comparison (3.33 B paired annotations; SpliceAI vs OpenSpliceAI rs10).

| Quantity | Value | Note |
|---|---|---|
| Paired annotations | 3,334,708,099 | 99,283 audited chunks, exact gene and allele |
| Max-DS correlation (Pearson) | 0.795 | Lin CCC 0.790 |
| Agreement @ 0.5 ($\kappa$) | 0.703 | overall agreement 99.90% is inflated by shared negatives |
| Positive-class Jaccard @ 0.5 | 0.542 | about half of all calls are shared |
| Loss agreement (AL / DL) | **0.900 / 0.906** | losses agree closely |
| Gain agreement (AG / DG) | **0.681 / 0.597** | gains do not |
| Call-rate ratio (OSAI / SpliceAI) | 0.44 @ 0.1 → 1.06 @ 0.8 | conservatism reverses with the cutoff |
| Position agreement @ 0.5 | 100.00% loss, 96.8–98.1% gain | among jointly called variants |
| MAE surviving ±0.005 rounding | 88.6% | precision is not the explanation |

<ZoomFigure src={calibration} alt="Five panels showing the distribution of the OpenSpliceAI score conditional on the SpliceAI score, for acceptor gain, acceptor loss, donor gain, donor loss and the maximum. Loss panels track the diagonal; gain panels stay near zero until the SpliceAI score exceeds about 0.5." variant="wide">
  <strong>Figure 4.</strong> Calibration of OpenSpliceAI against SpliceAI over 3.33 billion paired annotations. Each panel shows the distribution of the OpenSpliceAI score conditional on the SpliceAI score — orange line the conditional median, bands the central 50% and 90%. **Losses** (panels 2 and 4) track exact agreement closely. **Gains** (panels 1 and 3) do not: OpenSpliceAI's median stays near zero until SpliceAI is well above 0.5. Two SpliceAI columns are empty because a two-decimal value whose binary representation sits just below the bin edge lands one bin low; this affects 0.03% of observations and no reported threshold.
</ZoomFigure>

This is a concordance result against a comparator, not against truth. No experimentally validated splicing outcome enters the analysis, so it locates *where* and *how much* the two tools differ — and §4.5 shows that the difference is specific to gains rather than diffuse — but it cannot say which tool is right.

## 4. Genome-wide SNV re-scoring campaign

### 4.1 Motivation

To settle the mask attribution and produce a permanent, tool-neutral resource, we set out to re-score **every human GRCh38 SNV** with OpenSpliceAI and writing an `OpenSpliceAI=` delta-score field next to SpliceAI's existing field at every position — a strictly **mask-matched** overlay (OpenSpliceAI run with `-M 1` against SpliceAI's masked precompute). The result is an apples-to-apples comparison at every SNV in the genome, not a sampled subset, and a reusable precompute that others can query without a GPU.

### 4.2 Scale and cluster constraints

The input is partitioned into **100,000 chunks** of ~34,334 SNVs each — about **3.43 billion SNVs per model seed**. Each seed (we are running two independently drawn MANE seeds, **rs10** and **rs13**, scored as single models, not the 5-seed ensemble) is therefore a multi-thousand-GPU-hour job. Two hard limits on the shared cluster shaped the design (Fig. 5): the GPU QOS caps the account at **10 GPUs** (shared across all lab members), and the controller's `MaxJobCount` is ≈ 100,000 total array tasks (with `MaxArraySize` = 15,000). A naïve one-task-per-chunk submission (100,000 tasks for a single seed) alone exceeds the job-count cap.

### 4.3 Design

The campaign is driven by a single `campaign.sh {status|submit|resume|pause}` script over a per-chunk SLURM array, with four design choices:

- **`PER_TASK` chunks per task, sequential.** Each array task scores its chunks **one at a time** on a single GPU (batched inference already saturates the A100, so fanning out within a task would only OOM). At the current `PER_TASK = 10` that is 10,000 tasks per seed — comfortably under both array and job-count caps.
- **Idempotent at chunk granularity.** A chunk is skipped if its output already has the correct record count; a partial file from a killed task is detected by count mismatch, deleted, and redone. This makes `pause`/`resume` lossless: only the handful of chunks in flight on the GPUs at pause time are ever repeated.
- **Whole-campaign GPU cap by construction.** SLURM's `%N` array throttle is *per array job*, so two seed-arrays each at `%4` could use 8 GPUs. Instead, `submit` **serializes the seeds into a dependency chain** (`seed[i] --dependency=afterany:seed[i−1]`), so only one seed-array is ever runnable. One runnable array throttled at `%CONC` ⇒ **total concurrent GPUs ≡ CONC**. We set **`CONC = 6`** (initially 4) — a deliberately good-citizen cap that leaves the rest of the shared 10-GPU account free for labmates. The cap can be retuned on a *live* campaign without losing work via `scontrol update … ArrayTaskThrottle=N`.

<ZoomFigure src={campaignDesign} alt="Schematic of the campaign engine: 100,000 chunks feed array tasks of 20 chunks each on one GPU; seeds rs10 and rs13 are serialized by an afterany dependency and throttled at %4 so total concurrent GPUs equals CONC=4; list of cluster caps" variant="wide">
  <strong>Figure 5.</strong> Campaign design. The 100,000-chunk SNV space is scored in `PER_TASK`-chunk array tasks, one GPU each. The two seeds are serialized into an `afterany` dependency chain and each throttled at `%CONC`, so only one seed-array runs at a time and **total concurrent GPUs ≡ CONC** — well under the shared 10-GPU account cap. The boxed constraints are the cluster limits that forced this shape.
</ZoomFigure>

### 4.4 Current status

Scoring is **substantially complete for rs10 and paused for rs13**. A deep audit on 2026-08-08 — byte size, nanosecond mtime, record count, canonical record digest and raw SHA-256 of every input and output — found **99,283 of 100,000 chunks valid for rs10** (693 missing, 13 empty, 11 truncated) and **45,597 for rs13**, the latter covering chunks 1–46,920, i.e. chromosomes 1 through 7. That is roughly 3.41 billion scored source records for rs10 and 1.57 billion for rs13, and about 0.7 TB of output. Both remaining arrays have been pending since 2026-08-08 on `AssocGrpBillingMinutes`: the GPU account exhausted its billing allocation, which is a resource limit rather than a fault in the campaign. The rs10 coverage is already sufficient for the genome-wide analysis reported in §3.3 and §4.5; every result carries a `provisional` status by the analysis pipeline's own contract, because a `final` label requires the chunk domain to be exactly 1–100,000.

**Table 5.** Campaign scale and realised status.

| Quantity | Value |
|---|---|
| Chunks per seed | 100,000 (~34,334 SNVs each) |
| SNVs per seed | ~3.43 billion |
| Seeds scored | rs10, rs13 (single-model, mask-matched `-M 1`) |
| rs10 audit-valid chunks | **99,283 / 100,000** (3.41 B source records) |
| rs13 audit-valid chunks | **45,597 / 100,000** (chr1–chr7) |
| Output per seed | ~0.5 TB |
| Current state | rs10 and rs13 arrays pending on GPU billing allocation since 2026-08-08 |
| Analysis status | provisional — sufficient for genome-wide concordance (§3.3, §4.5) |

<ZoomFigure src={campaignProgress} alt="Campaign progress and throughput panels for the genome-wide SNV scoring campaign" variant="wide">
  <strong>Figure 6.</strong> Genome-wide SNV scoring — campaign progress and throughput at the self-imposed GPU cap. The deliverable is an `OpenSpliceAI=` delta-score field beside `SpliceAI=` at every human SNV.
</ZoomFigure>

### 4.5 Is the difference the method, or just the training run?

A difference between two trained networks is only interesting if it exceeds the difference between two training runs of the *same* network. Because the campaign scored two independently seeded MANE checkpoints, that control is available — and it can be run on an **identical chunk domain**: the 45,589 chunks scored by both seeds. Three quantities then become directly comparable on the same 1.54 billion paired annotations: rs10 versus rs13 (training seed), and SpliceAI versus each of them (method).

Overall, the method gap is real but of the same order as retraining: mean absolute difference 0.00285 between SpliceAI and OpenSpliceAI against 0.00108 between two seeds, a ratio of **2.65×** (1.45× in RMSE). Event by event, that summary breaks apart (Table 6, Figure 7). On **losses**, the method gap is barely larger than the seed gap — ratios of 1.13× and 1.17×, with between-tool correlations of 0.897 and 0.904 against between-seed correlations of 0.915 and 0.926. For loss-of-site interpretation, a different implementation is very nearly as interchangeable as a different training seed. On **gains**, the ratios are 3.35× and 3.92×.

This localises the entire OpenSpliceAI-versus-SpliceAI difference: it is not diffuse numerical drift, and it is not training-run variance. It is a specific, reproducible disagreement about **gain-of-site prediction**, which survives matching the mask, the annotation, the distance and the genomic domain. A plausible mechanism is that under masking a loss score is only reported where the shared annotation already declares a splice site — both tools answer "how much does this variant damage a site we both know about," and they agree — whereas a gain score asks "is there a *new* site here," a question with no annotation to anchor it. Restricting the genome-wide arm to the same chromosomes 1–7 changes every statistic by less than 0.002, so the control region is representative.

**Table 6.** Training-seed variation versus method difference, on an identical 45,589-chunk domain (1.54 B paired annotations).

| Event | Between two seeds (r / MAE) | SpliceAI vs OpenSpliceAI (r / MAE) | MAE ratio |
|---|---|---|---|
| Acceptor gain | 0.831 / 0.00040 | 0.686 / 0.00134 | **3.35×** |
| Acceptor loss | 0.915 / 0.00031 | 0.897 / 0.00035 | **1.13×** |
| Donor gain | 0.799 / 0.00033 | 0.604 / 0.00131 | **3.92×** |
| Donor loss | 0.926 / 0.00027 | 0.904 / 0.00031 | **1.17×** |
| Maximum | 0.892 / 0.00108 | 0.796 / 0.00285 | 2.65× |

<ZoomFigure src={seedVsMethod} alt="Two panels. Left: mean absolute difference per event between two OpenSpliceAI training seeds and between SpliceAI and each seed. Right: the ratio of method error to seed error per event, showing about 1.1 for losses and 3.3 to 3.9 for gains." variant="wide">
  <strong>Figure 7.</strong> Training-seed variation versus method difference, measured on an identical chunk domain. <strong>(A)</strong> Mean absolute difference per event between two OpenSpliceAI seeds (green) and between SpliceAI and each seed. <strong>(B)</strong> Their ratio: near 1 for losses — a different implementation is about as interchangeable as a different training seed — and 3.3–3.9× for gains.
</ZoomFigure>

One asymmetry remains by construction and is not separable here: SpliceAI's published scores average **five** models, while each OpenSpliceAI arm is a **single** checkpoint. The seed contrast shows a second checkpoint moves the scores appreciably, so an OpenSpliceAI ensemble would be expected to sit closer to SpliceAI than either seed alone. Testing that requires a third scored seed and is the natural next experiment.

## 5. Discussion

**Reimplementation as a platform.** The value of OpenSpliceAI is not a single new number but the conversion of a closed, human-only artifact into an open, end-to-end, PyTorch-native platform. Because one model class is parameterized by the flanking size and reused across `create-data`, `train`, `transfer`, `calibrate`, `predict`, and `variant`, a user can take the architecture to a new genome, retrain or fine-tune it, calibrate it, and score variants — none of which was supported by the original release. The published study [1] shows this most clearly on non-human species, where transfer learning yields the largest gains.

**Accuracy parity-plus, with a specificity tilt.** On human MANE the retrained models are not merely on par with SpliceAI; they exceed it on the canonical threshold-accuracy and AUPRC metrics while being more conservative (higher precision, slightly lower recall) — and there, against annotated splice sites, "more conservative" is a claim about accuracy that the test set can support. At the variant level the genome-wide comparison (§3.3) shows a related but more specific pattern: OpenSpliceAI issues fewer calls than SpliceAI at permissive delta-score cutoffs and marginally more at stringent ones, with the difference concentrated almost entirely in gain-of-site scores. Because that comparison has no truth set, it cannot be read as fewer false positives; a gain call only SpliceAI makes may equally be one OpenSpliceAI misses. Whether the tilt is desirable is application-dependent, and calibration (§2.8) plus the per-class delta scores let a user pick an explicit threshold rather than inherit one.

**Where two implementations of one architecture actually diverge.** The most transferable result here is not a headline correlation but a decomposition. By scoring two independently seeded checkpoints over an identical chunk domain, the campaign measures the difference between two *training runs* alongside the difference between two *implementations* — and finds they are nearly the same size for loss-of-site prediction (1.1×) and three to four times apart for gain-of-site prediction (§4.5). Loss scores are anchored: under masking they are reported only where the shared annotation already declares a splice site, so both tools answer a question with a fixed reference and they agree. Gain scores ask whether a *new* site exists, with nothing to anchor them, and that is where reimplementation, retraining data and architecture-level choices show through. Anyone porting an analysis between the two tools should expect the gain channel, not the loss channel, to move — and anyone benchmarking a splice predictor should report gains and losses separately, because a pooled score averages two very different regimes.

**Throughput as an enabler.** The batched, reference-deduplicated scorer is what turns "score every SNV in the genome" from a thought experiment into a managed campaign. The same engineering that makes the campaign fast also makes it a *good citizen*: by serializing seeds into a dependency chain, the whole multi-seed job provably never exceeds its configured GPU footprint on a shared account, and by being idempotent at chunk granularity it can be paused and resumed without waste. The genome-wide, strictly mask-matched overlay it produces is, itself, a reusable contribution: it lets any downstream user compare the two tools at a SNV of interest without recomputation, and it has already removed the unmasked-vs-masked confound that limited the earlier variant comparison.

## 6. Limitations

1. **Recall trade-off.** OpenSpliceAI's higher precision comes with slightly lower recall than SpliceAI (§3.1); applications that weight sensitivity above specificity should choose the operating point deliberately.
2. **The variant comparison is concordance, not accuracy.** The genome-wide comparison in §3.3 and §4.5 is now mask-, annotation- and distance-matched, which removes the confound that limited the earlier 452 K-SNV analysis. It still measures agreement against SpliceAI as a comparator, not against experimentally validated splicing outcomes, so it identifies where the two tools differ but cannot say which is correct. It is also a single OpenSpliceAI checkpoint against a five-model SpliceAI ensemble, and that asymmetry is not separable from the method difference.
3. **Single-device training.** Despite a hardcoded `N_GPUS = 2` in the hyperparameter tables, the code does not use `DataParallel`; training runs on one device, and `N_GPUS` only scales batch size (§2.4).
4. **Maintenance hazard.** The $(W, AR, \text{batch size})$ schedule is duplicated across several subcommands; a change in one must be mirrored in the others. This is the most likely source of subtle bugs and is flagged in the codebase.
5. **Campaign incomplete.** The overlay covers 99,283 of 100,000 chunks for rs10 and 45,597 for rs13; both arrays are pending on the GPU account's billing allocation. Every analysis result is therefore labelled `provisional` by contract, and the rs13-dependent comparisons cover chromosomes 1–7 rather than the whole genome — a contiguous region, not a random sample, though restricting the genome-wide arm to it changes every statistic by less than 0.002.

## 7. Availability and reproducibility

OpenSpliceAI is open-source ([github.com/Kuanhao-Chao/OpenSpliceAI](https://github.com/Kuanhao-Chao/OpenSpliceAI)) with documentation at [ccb.jhu.edu/openspliceai](https://ccb.jhu.edu/openspliceai/). It installs from PyPI (`pip install openspliceai`, v0.0.7; Python ≥ 3.9, PyTorch ≥ 2.2.1) and exposes one console command with six subcommands. Typical invocations:

```
# build training data, then train a 10,000-nt model
openspliceai create-data --annotation-gff anno.gff --genome-fasta genome.fa --output-dir data/
openspliceai train --flanking-size 10000 --train-dataset data/dataset_train.h5 --output-dir run/

# fine-tune onto a new species (freeze all but the last residual units)
openspliceai transfer --pretrained-model run/models/model_best.pt --flanking-size 10000 ...

# score a genome and annotate a VCF (directory => 5-seed ensemble)
openspliceai predict --model models/openspliceai-mane/10000nt/ --input-sequence genome.fa ...
openspliceai variant -R genome.fa -A grch38 --model models/openspliceai-mane/10000nt/ -b 128 < in.vcf > out.vcf
```

The **model zoo** ships five random-seed checkpoints per setting — `models/openspliceai-{mane,mouse,zebrafish,arabidopsis,honeybee}/{80,400,2000,10000}nt/model_*nt_rs{10..14}.pt` — intended to be ensembled by pointing `predict`/`variant` at the directory; `models/spliceai/` holds the original Keras weights for the `--model-type keras` path. The accuracy benchmark harness streams a MANE `dataset_test.h5` through both ensembles and computes the canonical top-kL/AUPRC statistics on the full test set. The genome-wide campaign is driven by `campaign.sh {status|submit|resume|pause}` over a per-chunk SLURM array, with all parameters (paths, `-M 1 -f 10000 -D 50 -b 128`, seeds, `PER_TASK`, `CONC`) centralized in one `config.sh`; the campaign is idempotent and pause/resume-safe by construction. The figures in this report are regenerated from the on-disk benchmark JSONs and live campaign status by a single self-contained plotting script.
