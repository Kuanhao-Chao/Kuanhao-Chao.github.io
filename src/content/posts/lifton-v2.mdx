---
title: 'LiftOn 2.0: scaling accurate genome annotation to whole genomes'
description: 'LiftOn 2.0 rebuilds our DNA-plus-protein genome-annotation lift-over tool for whole-genome scale: lower memory, fewer crashes, broader feature recovery, and a safer merge — validated on seventeen whole-genome end-to-end lift-overs across a four-tier divergence ladder, from same-species to mammal-to-bird.'
authors: ['Kuan-Hao Chao']
abstract: >-
  LiftOn 2.0 is a major update to the genome-annotation lift-over tool that combines DNA alignment
  (Liftoff) and protein alignment (miniprot). The core idea is unchanged: use both signals, then choose
  the gene model that best preserves the reference protein. The update focuses on making that idea reliable
  at whole-genome scale. It adds a memory-bounded windowed aligner for giant genes, a best-of-outcome merge
  that keeps the DNA-plus-protein model only when it improves the emitted protein, default lifting of
  gene-like feature types beyond protein-coding genes, full-genome robustness fixes, and faster execution
  protected by a 24-cell byte-identity regression matrix. Evaluated on seventeen whole-genome, end-to-end
  lift-overs spanning a four-tier divergence ladder — from same-species plant and animal assemblies, through
  close (human-to-gorilla, mouse-to-Mus caroli) and distant (dog-to-cat, human-to-macaque) cross-species
  pairs, to very-distant pairs (eudicot-to-monocot, mammal-to-fish, mammal-to-bird) where DNA synteny has all
  but vanished — LiftOn 2.0 produced higher mean protein identity than both single-method baselines (Liftoff
  and miniprot) on every one of the seventeen whole genomes; against Liftoff the lead widens steadily with
  divergence, and read apples-to-apples on the transcripts both tools recover, LiftOn's per-transcript edge over
  miniprot is genuine and peaks on the distant tier — at very-distant divergence its higher aggregate reflects
  high-precision recovery of the small DNA-anchored core it places, not higher recall. On five RefSeq genomes that the previous release crashed on, it finished the runs and
  recovered tens of thousands more coding transcripts, and it emitted cleaner GFF3 than v1.0.8 on every genome
  both produced.
date: 2026-06-20
category: summary
tags: ['Genome annotation', 'Comparative genomics', 'Open source']
linkLabel: 'LiftOn 2.0'
image: ../../assets/posts/lifton-v2/fig_full_accuracy.png
logo: ../../assets/logos/software/lifton.png
imageAlt: "LiftOn 2.0's whole-genome accuracy: four tools compared on all seventeen full-genome lift-overs, with LiftOn 2.0 leading both single-method baselines on every genome and the lead over the best single method growing with divergence."
references:
  - text: 'Chao, K.-H. et al. Combining DNA and protein alignments to improve genome annotation with LiftOn. Genome Research (2025).'
    doi: 'https://doi.org/10.1101/gr.279620.124'
  - text: 'Shumate, A. and Salzberg, S. L. Liftoff: accurate mapping of gene annotations. Bioinformatics (2021).'
    doi: 'https://doi.org/10.1093/bioinformatics/btaa1016'
  - text: 'Li, H. Protein-to-genome alignment with miniprot. Bioinformatics (2023).'
    doi: 'https://doi.org/10.1093/bioinformatics/btad014'
---
import ZoomFigure from '../../components/ZoomFigure.astro';
import figAccuracy from '../../assets/posts/lifton-v2/fig_full_accuracy.png';
import figCompleteness from '../../assets/posts/lifton-v2/fig2_completeness.png';
import figValidity from '../../assets/posts/lifton-v2/fig_full_validity.png';
import figPerformance from '../../assets/posts/lifton-v2/fig3_performance.png';

[LiftOn](/posts/lifton/) was built around a simple observation: DNA alignment and protein alignment fail in different ways. DNA lift-over with [Liftoff](https://doi.org/10.1093/bioinformatics/btaa1016) preserves gene structure well when two genomes are close, but it can introduce frameshifts, premature stops, and shifted exon boundaries as sequence diverges. Protein-to-genome alignment with [miniprot](https://doi.org/10.1093/bioinformatics/btad014) survives divergence better, but it can be fragmentary and does not naturally preserve the reference transcript structure.

LiftOn combines the two. For each gene, it compares the Liftoff model and the miniprot model against the reference protein, chains together the coding segments that best preserve that protein, and then runs an open-reading-frame rescue step to repair boundary problems. The first release showed that this dual-evidence strategy improves protein-coding annotation transfer across genomes and across species.

**LiftOn 2.0** keeps that scientific idea, but rebuilds the tool for the way people now use genome annotation: whole RefSeq genomes, complete assemblies, and many target species at once. The update is less about adding a new headline method and more about making the method dependable at scale.

## What changed

The first release was accurate, but it was engineered around chromosome-scale runs. Whole genomes exposed four practical problems. A few giant genes could drive full dynamic-programming alignment into tens of gigabytes of memory. Some full plant genomes crashed partway through, leaving partial annotations that looked usable but were not complete. The pipeline spent too much time in serial execution and disk round-trips. And the default lift covered protein-coding genes while quietly leaving pseudogenes, long non-coding RNA genes, tRNAs, rRNAs, and other gene-like features behind.

LiftOn 2.0 addresses those problems with four main updates.

First, it replaces the most memory-hungry alignments with an **anchor-windowed aligner**. LiftOn scores candidate genes by aligning their translated proteins back to the reference protein. A full Needleman-Wunsch alignment with traceback is quadratic in memory, so one giant gene can dominate the entire run. The new aligner finds exact shared anchors, chains them into a consistent backbone, and runs full dynamic programming only inside the windows between anchors. For ordinary homologous genes, this keeps the same answer while bounding peak memory. On the largest memory cases, the practical change is dramatic: the benchmark shows controlled same-input runs using up to about 24x less memory, and the most memory-intensive four-way mouse subset drops from 44.6 GB in v1.0.8 to 0.76 GB in v2.0.

Second, it makes the DNA-plus-protein merge more conservative. The old LiftOn always emitted the merged model after protein maximization. That is usually right, but a local improvement can still perturb a downstream reading frame. LiftOn 2.0 now evaluates the merged model and the DNA-only model after ORF rescue, then emits the one with the better reference-protein identity. In other words, miniprot evidence is used when it improves the final transcript, not simply because it exists. On identical aligner inputs, that change has the shape we wanted: many improvements and almost no regressions, including 115 improved transcripts and 1 regressed transcript on the fruit-fly benchmark, and 83 improved with 0 regressed on mouse-to-rat.

Third, LiftOn now lifts **every gene-like feature type** by default. Instead of assuming only `gene` parents matter, it scans the reference annotation for top-level feature types that behave like genes: a parent record with transcript and exon children. That brings pseudogenes, non-coding RNA genes, tRNAs, rRNAs, snoRNAs, snRNAs, and related structured features into the lift. The change matters because these features are part of the annotation, not decoration. Dropping them means downstream analyses inherit a narrower genome map than the reference actually provides.

Fourth, the execution engine was rebuilt under a byte-identity contract. The two external aligners can run concurrently. The per-locus merge can run across worker threads. Some intermediate files can be streamed or passed in memory. miniprot now receives the requested thread count. These are engineering changes, so they should not silently change the annotation. LiftOn 2.0 protects that boundary with a 24-cell regression matrix: every combination of streaming, in-memory Liftoff handoff, thread count, and native bindings must reproduce the default GFF3 byte-for-byte. If a speed path changes one byte of output, it fails.

## What the benchmark shows

I evaluated LiftOn 2.0 the way the tool is actually used: seventeen whole-genome, end-to-end lift-overs, each tool running from scratch on a complete target assembly. The seventeen are organized as a four-tier divergence ladder — same-species (independent assemblies and cultivars of Arabidopsis, honeybee, rice, maize, tomato), close cross-species (human to gorilla, mouse to *Mus caroli*, tomato to potato), distant cross-species (dog to cat, human to macaque, human to marmoset), and very-distant (Arabidopsis to rice eudicot-to-monocot; human to zebrafish, chicken, and frog; Drosophila to honeybee), where genome-wide DNA synteny has all but disappeared. The comparison includes four tools: Liftoff alone, miniprot alone, LiftOn v1.0.8, and LiftOn 2.0. Every output is re-scored by the same evaluator rather than by each tool's own reporting: for coding transcripts, the evaluator translates the lifted CDS, aligns it to the reference protein, and computes mean protein identity. (A companion suite of single-chromosome subsets, used only to isolate the engine's speed and memory, lives in the technical report.)

On all seventeen whole genomes, LiftOn 2.0 produced a higher mean protein identity than both single-method baselines, and the set-mean margin over the better of the two grew with divergence — from +0.0007 on same-species Arabidopsis to +0.13 at the mammal-to-bird extreme (human to chicken). Two caveats keep that honest. First, against Liftoff — the DNA baseline LiftOn extends — the win is unambiguous: as the target diverges the DNA model frameshifts and Liftoff collapses from about 0.99 to 0.15–0.31 across the very-distant tier, while the protein model survives and the merge keeps whichever signal is locally better. Second, that headline margin is each tool's mean over its own recovered set; on the very-distant tier, where LiftOn and miniprot recover very different fractions of the genome, it is partly a denominator effect — the apples-to-apples reading (below) is more nuanced. Against the previous release, LiftOn 2.0 matched or beat v1.0.8 on the great majority of the genomes both completed — every same-species, cross-species, close, and distant pair — with only fractional dips on the three most extreme distances (human to zebrafish, chicken, and frog, each about 0.0002–0.002, on sets both recover at under 25 percent). On Arabidopsis, rice, maize, and two tomato pairs, v1.0.8 never produced a comparable number at all — it crashed mid-run (more below).

<ZoomFigure src={figAccuracy} alt="Two-panel whole-genome accuracy figure. (A) Four-way mean protein identity on each of the seventeen whole genomes; LiftOn 2.0 is at or above both single-method baselines everywhere, while the DNA-only Liftoff collapses on the very-distant pairs. (B) LiftOn 2.0 minus the best single baseline per whole genome, positive on all seventeen and largest at the mammal-to-bird and mammal-to-amphibian extremes." variant="wide">
  <strong>Figure 1.</strong> Whole-genome accuracy across the four-tier divergence ladder. <strong>(A)</strong> Mean protein identity (each tool over its own recovered set); LiftOn 2.0 leads both single-method baselines on every whole genome, while the DNA-only Liftoff falls from about 0.99 to 0.15–0.31 as synteny disappears. <strong>(B)</strong> LiftOn 2.0's set-mean lead over the best single method is positive on all seventeen and widens with divergence, to +0.13 at mammal-to-bird — but on the very-distant tier this set-mean view is partly a denominator effect; apples-to-apples, the per-transcript edge over miniprot peaks on the distant tier (see below).
</ZoomFigure>

The largest practical result is not an accuracy decimal. It is that LiftOn 2.0 finishes full genomes that v1.0.8 abandoned — five of the seventeen. On full RefSeq *Arabidopsis*, v1.0.8 crashed after recovering 13,618 coding transcripts, about 28% of the reference set; LiftOn 2.0 completed the run and recovered 48,207, adding 34,589. On full rice it stopped at 32,933, about 77%; LiftOn 2.0 finished with 42,527, adding 9,594. On maize and two tomato genomes, v1.0.8 crashed with no usable whole-genome output at all, while LiftOn 2.0 completed each. A separate hardening fix matters here too: on the dog-to-cat lift, a single malformed transcript — one with inverted coordinates produced by the ORF-rescue step — used to abort the entire run partway through; LiftOn 2.0 now drops that one transcript with a logged warning and finishes the genome, 61,276 transcripts instead of 11,048. The lesson the whole-genome benchmark kept teaching is that one bad feature must never be allowed to sink an entire genome.

The same figure also shows the broader feature lift. On full *Arabidopsis*, LiftOn 2.0 recovers thousands of gene-like features that v1.0.8 omitted by design: 4,816 pseudogenes, all 3,878 lnc_RNA genes in the reference set, many more tRNAs and snoRNAs, and nearly all rRNA and antisense RNA records. This does not mean every nested product is solved. Mature miRNA recovery remains poor in this release because those records are nested under primary transcripts in a way the current transcript-centric logic does not yet handle well. But the parent gene-like features now come through, and the annotation is much less artificially coding-only.

<ZoomFigure src={figCompleteness} alt="Two-panel completeness figure. (A) Grouped bars of coding transcripts recovered on full Arabidopsis and rice genomes for LiftOn v1.0.8 versus 2.0; v1.0.8 reaches only 28 percent and 77 percent before crashing while 2.0 reaches about 100 percent. (B) Horizontal grouped bars of features recovered by type on the full Arabidopsis genome, with v2.0 recovering pseudogenes, lnc_RNA genes, tRNAs, rRNAs, and other feature classes that v1.0.8 dropped." variant="wide">
  <strong>Figure 2.</strong> Whole-genome robustness and feature breadth. LiftOn 2.0 completes full Arabidopsis and rice runs that v1.0.8 abandoned, and it lifts gene-like feature classes beyond protein-coding genes.
</ZoomFigure>

Completeness still needs a careful reading, and the whole genomes make the trade-off concrete. On the same-species through distant genomes, every tool that finishes recovers most or all of the coding set. On the five very-distant genomes, miniprot recovers far more coding transcripts than LiftOn — roughly 67 to 86 percent versus LiftOn's 4.5 to 24 percent — because it searches directly with proteins and is not anchored to a DNA lift. LiftOn's goal is different: keep reference-consistent transcript structure and emit accurate gene models where a reliable lift exists. On raw recall alone, protein search wins on the hardest pairs; on the accuracy of the transcripts it does recover, and on whole-genome robustness, the combined approach is the stronger default. (There is a smaller mirror of this trade-off on the close and distant mammalian pairs, where LiftOn recovers a fraction of a percent fewer transcripts than Liftoff — the recall it gives up for the accuracy lead.)

The very-distant whole genomes sharpen the picture — and demand a careful reading of what the accuracy numbers mean. Where DNA synteny is essentially gone, Liftoff's DNA-only model collapses to between 0.15 and 0.31 mean protein identity while LiftOn 2.0 holds far higher, so against Liftoff its lead is largest exactly here. But the set-mean lead over the *best* single method (which at this distance is miniprot) is mostly a denominator effect: LiftOn recovers only the small DNA-anchored core (about 4.5 to 24 percent of coding transcripts) and averages over that easy set, while miniprot recovers far more (about 67 to 86 percent) and its mean is pulled down by the larger, harder set. The fair comparison is on the transcripts both tools place. Read that way, LiftOn's per-transcript edge over miniprot is real and peaks on the *distant* tier (dog-to-cat, human-to-macaque, human-to-marmoset, +0.023 to +0.032), but it does *not* hold at very-distant — on four of the five very-distant genomes miniprot is as accurate or slightly more so on the shared transcripts (human-to-chicken, human-to-frog, human-to-zebrafish, Arabidopsis-to-rice). What *is* a clean per-transcript win at extreme distance is LiftOn 2.0 over v1.0.8, which recover the same small set and so compare without that artifact. The honest summary: at extreme distance LiftOn delivers high-precision refinement of the recoverable core, not higher recall — and closing the recall gap with a miniprot-only rescue path is the clear next step.

Output validity improves where scale stresses the pipeline. Re-running every output through one GFF3 validator, LiftOn 2.0 emits fewer errors than v1.0.8 on every genome both versions produced — often far fewer (honeybee 103 to 53, Drosophila 150 to 100, dog-to-cat 159 to 107, Arabidopsis-to-rice 226 to 136) — even though it now lifts the larger gene-like feature set. The honest caveat is that where LiftOn lifts the full feature set onto a divergent or repeat-rich genome, it carries somewhat more validation load than Liftoff's stripped-down output. A lift-over tool is only useful if its accuracy claims and its failure modes are both explicit.

<ZoomFigure src={figValidity} alt="A horizontal grouped bar chart of GFF3-validate error counts for LiftOn v1.0.8 versus LiftOn 2.0 across the seventeen whole genomes; LiftOn 2.0 is lower on every one both versions produced, with the reduction annotated on each." variant="wide">
  <strong>Figure 3.</strong> Output validity across the seventeen whole genomes. LiftOn 2.0 emits fewer GFF3 errors than v1.0.8 on every whole genome both versions produced, despite lifting the larger gene-like feature set.
</ZoomFigure>

Finally, the performance numbers explain why the new version changes practical use. On identical cached aligner inputs — a controlled comparison that isolates LiftOn's own code — v2.0 is faster end to end and much lighter on memory: on the most memory-intensive case it peaks at 0.76 GB against v1.0.8's 44.6 GB, a 59-times reduction, and runs up to 2.15 times faster. The memory reductions come from the windowed aligner, the speedups from concurrent aligners, per-locus threading, and less disk and database overhead. On a fresh whole-genome run v2.0 can do more total work than v1.0.8 because it lifts the expanded feature set, so wall time can rise; the decisive point is that the memory bound and the robustness fixes are what make those whole-genome runs finish at all.

<ZoomFigure src={figPerformance} alt="Two-panel performance figure. (A) Grouped bars of end-to-end wall-clock time for LiftOn v1.0.8 versus 2.0 on several datasets, with 2.0 faster everywhere and speedup factors labeled. (B) Grouped bars of peak memory on a logarithmic scale for the same runs, with 2.0 using far less RAM and fold-reductions labeled up to about 24 times." variant="wide">
  <strong>Figure 4.</strong> Faster and lighter on identical inputs. The execution changes reduce wall time, and the windowed aligner drives the large peak-memory drop.
</ZoomFigure>

## What changes for users

The basic command is unchanged:

`lifton -g reference.gff3 reference.fa target.fa -o lifton.gff3`

The defaults are more complete and more conservative. LiftOn 2.0 lifts all gene-like features by default, uses the best-of-outcome merge, and uses the memory-bounded aligner. If you need to reproduce the published LiftOn 1.x behavior, the relevant opt-out flags are `--gene-only`, `--legacy-merge`, `--full-dp-align`, and `--serial-aligners`.

For larger runs, `--threads N --locus-pipeline` enables the threaded per-locus path, while `--stream` and `--inmemory-liftoff` reduce intermediate file overhead. These speed paths are covered by the byte-identity matrix, so they are performance choices rather than analysis choices: the annotation should be byte-for-byte identical to the default output.

The main thing I want users to take from LiftOn 2.0 is that genome annotation lift-over needs both biological evidence and software discipline. The biological argument is still DNA plus protein: each signal catches errors the other misses. The engineering argument is now just as important: the tool has to finish full genomes, fit in realistic memory, lift the annotation types people expect, and make performance refactors prove that they did not change the science.

LiftOn 2.0 is free and open source. Browse the [code](https://github.com/Kuanhao-Chao/LiftOn), work through the [documentation](https://ccb.jhu.edu/lifton/), or read the [Genome Research paper](https://doi.org/10.1101/gr.279620.124) behind the original LiftOn method. LiftOn was built with Jakob M. Heinz, Celine Hoh, Alan Mao, Alaina Shumate, Mihaela Pertea, and Steven Salzberg at Johns Hopkins.
