---
title: 'Revisiting WGT: a verified recognizer, a faster solver, and graph repair'
description: 'A verification-and-improvement pass over the Wheeler Graph Toolkit: an independent brute-force oracle and differential testing that harden the recognizer to zero false-accepts and full agreement with ground truth, a sparser SMT encoding that speeds up the full-range solver, and a new module that repairs a non-Wheeler DAG into a Wheeler graph while preserving its path-string set.'
authors: ['Kuan-Hao Chao']
abstract: >-
  The Wheeler Graph Toolkit (WGT) generates, recognizes, and visualizes Wheeler graphs — the class of
  edge-labeled graphs that can be indexed and searched like a BWT, and a foundation for compact pangenome
  indexes. Recognition is NP-complete, and WGT's recognizer (Wheelie) makes it practical by pairing a
  renaming heuristic with a permutation or SMT solver. This update is not a new method but a verification
  and engineering pass that makes the existing one more trustworthy, faster, and more capable. I built an
  independent brute-force oracle that enumerates every node ordering and checks the Wheeler axioms directly,
  then differential-tested the recognizer against it across thousands of graphs; that process hardened the
  recognizer's accept paths to zero false-accepts on a deliberately reject-heavy corpus and full agreement
  with the oracle on every decided graph (2,447 of 2,447, zero disagreements). I rebuilt the exponential
  reference implementation into an explicit axiom-checking decision procedure so it can serve as a
  trustworthy second opinion, sharpened the full-range SMT encoding so its setup is roughly an order of
  magnitude cheaper and its end-to-end time about two times faster on real DNA graphs, and added a graph
  repair module that turns a non-Wheeler DAG into a Wheeler graph while preserving the exact set of strings
  it spells. The theme throughout is to keep three questions separate — is a verdict correct, can it be
  produced at all, and how fast is it — because a faster wrong answer is not progress.
date: 2026-06-20
category: summary
tags: ['Pangenomics', 'Algorithms', 'Formal methods', 'Open source']
linkLabel: 'WGT, revisited'
image: ../../assets/posts/wgt-verified/fig_correctness.png
logo: ../../assets/logos/software/wgt.png
imageAlt: 'The verification result: on a reject-heavy corpus of non-Wheeler graphs the hardened recognizer produces zero false-accepts across every backend, and it agrees with an independent brute-force oracle on all 2,447 decided graphs with no disagreements.'
references:
  - text: 'Chao, K.-H., Chen, P.-W., Seshia, S. A., and Langmead, B. WGT: Tools and algorithms for recognizing, visualizing and generating Wheeler graphs. iScience (2023).'
    doi: 'https://doi.org/10.1016/j.isci.2023.107402'
  - text: 'Gagie, T., Manzini, G., and Sirén, J. Wheeler graphs: A framework for BWT-based data structures. Theoretical Computer Science (2017).'
    doi: 'https://doi.org/10.1016/j.tcs.2017.06.016'
  - text: 'Gibney, D. and Thankachan, S. V. On the hardness and inapproximability of recognizing Wheeler graphs. ESA (2019).'
    doi: 'https://doi.org/10.4230/LIPIcs.ESA.2019.51'
---
import ZoomFigure from '../../components/ZoomFigure.astro';
import figOverview from '../../assets/posts/wgt/wgt-overview.png';
import figCorrectness from '../../assets/posts/wgt-verified/fig_correctness.png';
import figCapability from '../../assets/posts/wgt-verified/fig_capability.png';
import figPerformance from '../../assets/posts/wgt-verified/fig_performance.png';
import figScaleRepair from '../../assets/posts/wgt-verified/fig_scale_repair.png';

[WGT](/posts/wgt/), the Wheeler Graph Toolkit, was built around a useful fact: a special class of graphs — **Wheeler graphs** — can be indexed and searched with the same machinery that makes the Burrows–Wheeler Transform and the FM-index fast. That makes them a natural backbone for graph-based pangenome indexes. The catch is that deciding whether an arbitrary graph *is* a Wheeler graph is NP-complete, so WGT's recognizer, **Wheelie**, makes the problem tractable by first running a renaming heuristic that narrows each node's possible position, and only then handing the much smaller leftover problem to a permutation search or an SMT solver.

This update keeps that method exactly as it is. It is not a new algorithm; it is a verification and engineering pass on the existing one, with three goals: make the recognizer's verdicts **trustworthy** by checking them against an independent ground truth, make the full-range SMT solver **faster** with a leaner encoding, and add the ability to **repair** a graph that is not a Wheeler graph into one that is. The work was less about a headline result and more about putting a foundational tool on a firmer footing.

<ZoomFigure src={figOverview} alt="The WGT pipeline: a generator emits a graph, the Wheelie recognizer applies a renaming heuristic before handing off to a permutation or SMT solver, and a visualizer draws the ordered graph." variant="landscape">
  <strong>Figure 1.</strong> The Wheeler Graph Toolkit, end to end. A <strong>generator</strong> produces a graph; the <strong>recognizer</strong>, Wheelie, decides whether it is a Wheeler graph and, if so, finds a valid node ordering — first applying a renaming heuristic, then handing the remainder to a permutation or SMT solver; and a <strong>visualizer</strong> draws the result. This pass strengthens the recognizer and adds a fourth capability: repair.
</ZoomFigure>

## What changed

A recognizer is only as useful as it is trustworthy: if it ever calls a non-Wheeler graph a Wheeler graph, every downstream conclusion built on that answer is unsupported. So the first and most important change is not a speedup — it is a way to *know* the answers are right.

First, I built an **independent brute-force oracle**. It is deliberately simple, so that it is obviously correct: for a graph with *n* nodes it enumerates all *n*! node orderings and, for each, checks the three Wheeler-graph axioms directly against the graph's actual edges. That is hopelessly slow — it is only usable up to about nine nodes — but on those small graphs it is an incontrovertible source of truth, computed by completely different code from the recognizer. With the oracle in hand I wrote a differential-testing harness that generates large families of graphs (De Bruijn graphs, tries, reverse-deterministic graphs, and random graphs, in both DNA and protein alphabets, plus hand-built edge cases with self-loops and parallel edges) and compares the recognizer's verdict to the oracle's on every one.

That comparison is what hardened the recognizer. It surfaced a handful of edge cases in the accept paths, and each was tightened: the permutation backend now explicitly rejects when its pruned search is exhausted rather than falling through; the fast path that settles an ordering by propagation alone now re-checks the axioms before accepting; an integer-overflow guard was added to the solver-dispatch heuristic; and the SMT path now guards degenerate inputs (empty or isolated-node graphs) that previously tripped the solver. After this hardening the recognizer agrees with the oracle on **every decided graph** — 2,447 of 2,447, with zero disagreements — and, on a corpus deliberately stuffed with non-Wheeler graphs to stress exactly the accept paths, it produces **zero** false-accepts across all of its backends.

Second, I rebuilt the **exponential reference implementation** into an honest decision procedure. WGT ships a reference implementation of the only previously proposed recognition algorithm (Gibney–Thankachan) so that the fast recognizer can be checked against an independent second opinion. To make that second opinion genuinely independent and decisive, I rewrote its core so that it enumerates orderings and verifies the three axioms against the graph's real edges — exactly mirroring the brute-force oracle — and returns a clean three-way verdict (Wheeler / not a Wheeler graph / undecided within its budget). It is now a trustworthy cross-check rather than a timing curiosity.

Third, I sharpened the **full-range SMT encoding**. WGT's `-f` mode skips the range-narrowing heuristic and hands the entire ordering space to the SMT solver — useful as an independent path and for graphs where you want no heuristic in the loop. The bottleneck there is how many constraints the encoding emits. The new encoding expresses the same axioms with far fewer terms: the cross-label "smaller label means earlier head" constraint drops from quadratic in the number of edges to roughly linear, and the within-label ordering constraint drops from quadratic in a label's edges to roughly quadratic only in its *distinct* endpoints. The two encodings are logically equivalent — they accept and reject exactly the same graphs — but the leaner one is much cheaper to build and to solve. The honest cost is memory: on graphs small enough that the old encoding never exploded, the new one uses about twice the RAM, because it trades a large number of constraints for a smaller number of auxiliary variables. It is a time-for-space trade on those graphs, not a free win, and the benchmark reports it as such.

Fourth, WGT can now **repair** a graph. Given a directed acyclic graph that is *not* a Wheeler graph, the new module unfolds it into the trie of its path-strings. A trie is always a Wheeler graph — you order its nodes by the strings spelled along incoming paths — so this always succeeds for a DAG, and crucially it preserves the **set of strings** the graph spells, exactly. Every repair self-verifies three ways: the recognizer accepts the result, the oracle accepts it when it is small enough, and the set of path-strings is confirmed identical to the input's. This turns "your graph is not a Wheeler graph" from a dead end into a constructive next step.

## What the benchmark shows

I evaluated each change against the question it is meant to answer, and kept those questions separate. Conflating them is how tools end up looking better than they are: a recognizer that returns answers quickly but sometimes returns the wrong one has not improved. So the results below are organized along three independent axes — **correctness** (is the verdict right?), **capability** (can a verdict be produced at all?), and **performance** (how fast, and how large, when it is right?).

On **correctness**, the picture is clean. On the reject-heavy corpus of non-Wheeler graphs — built specifically to probe the accept paths — the hardened recognizer produces zero false-accepts on every backend, and it agrees with the independent oracle on all 2,447 graphs both can decide, with no disagreements at all.

<ZoomFigure src={figCorrectness} alt="Two panels. (A) Bar chart of false-accepts (non-Wheeler graphs wrongly called Wheeler) by backend on a reject-heavy corpus: the hardened recognizer is at zero everywhere, with the pre-hardening behavior shown for contrast. (B) Verdict-agreement matrix versus the brute-force oracle: every backend matches the oracle on all real and synthetic graphs, with zero disagreements across 2,447 graphs." variant="wide">
  <strong>Figure 2.</strong> Correctness against an independent ground truth. <strong>(A)</strong> False-accepts by backend on a deliberately reject-heavy corpus (red = pre-hardening, blue = hardened): the hardened recognizer reaches zero false-accepts on every path. <strong>(B)</strong> Verdict agreement with the brute-force oracle — every backend matches the oracle on all 2,447 graphs it can decide (1,303 Wheeler, 1,144 non-Wheeler), with zero disagreements.
</ZoomFigure>

On **capability**, the rebuilt exponential reference now does something it could not before: it returns real verdicts where it used to run out of budget. On the subset of the benchmark small enough for the oracle to decide (931 graphs with at most nine nodes), the honest decision procedure decides all 931 — including seven non-Wheeler graphs built from real human gene orthologs that the previous reference could not classify within its budget. That is a capability gain, not a timing one: these are answers that simply did not exist before, regardless of how long you waited.

<ZoomFigure src={figCapability} alt="A stacked bar chart over graph families showing, for the previous exponential reference, how many graphs it could decide within its budget versus how many it left undecided; a red cap marks seven non-Wheeler biological graphs that the rebuilt decision procedure now decides, and a side note lists those seven human-ortholog graphs." variant="wide">
  <strong>Figure 3.</strong> Capability of the rebuilt exponential reference. Across the families in the oracle-decidable subset, the honest decision procedure now returns a verdict on every one of the 931 graphs, including seven non-Wheeler graphs built from real human gene orthologs (highlighted) that the previous reference left undecided — verdicts that did not exist before.
</ZoomFigure>

On **performance**, the leaner `-f` encoding does what it was designed to do. On the headline case — a De Bruijn graph built from a real gene at *k*=5, with around a thousand edges — building the SMT problem drops from about 2.1 seconds to about 0.14, roughly a fourteen-fold reduction, and the end-to-end full-range time drops from about 15 seconds to under 7, a bit better than two-fold. Across a corpus of 289 real DNA graphs the median full-range run is about 1.5 times faster, with no graph slower than before beyond measurement noise. None of this touches the **default** recognizer, which uses the heuristic and decides these same graphs near-instantly; the `-f` path is the no-heuristic stress path, and it is now meaningfully lighter.

<ZoomFigure src={figPerformance} alt="Two panels. (A) Stacked bars splitting full-range SMT time into setup and solve for the previous and new encodings on a real gene graph: setup falls about fourteen-fold and total time about two-fold. (B) Per-graph scatter of new-versus-old full-range time over a corpus of real DNA graphs, with most points below the diagonal, meaning the new encoding is faster." variant="wide">
  <strong>Figure 4.</strong> The leaner full-range SMT encoding. <strong>(A)</strong> Setup-versus-solve split on a real *k*=5 gene graph: encoding setup drops roughly fourteen-fold (≈2.1 s → ≈0.14 s) and total full-range time about two-fold (≈15 s → ≈7 s). <strong>(B)</strong> Per-graph full-range time, new versus old, over 289 real DNA graphs — points below the diagonal are faster under the new encoding, which is essentially all of them.
</ZoomFigure>

Finally, two practical questions: how large a graph can the recognizer handle, and does repair actually work? Under a ten-minute-per-graph budget, the default heuristic-plus-SMT recognizer decides synthetic worst-case graphs of around 2,800 nodes — far past what the permutation backend or the full-range path reach, and orders of magnitude past the exponential reference's hard limit. And repair is robust: across 820 random DAGs, every one that needed repair was repaired and verified, with no failures.

<ZoomFigure src={figScaleRepair} alt="Two panels. (A) Bar chart of the largest graph each backend can recognize within the time budget, on a log scale: the default heuristic-plus-SMT recognizer reaches the largest sizes by a wide margin, with the full-range and permutation paths and the exponential reference progressively lower. (B) Histogram of node blow-up after repair over 820 DAGs, with a large spike at one (graphs already Wheeler) and a spread on either side." variant="wide">
  <strong>Figure 5.</strong> Scale and repair. <strong>(A)</strong> Largest graph recognized within a ten-minute budget, by backend (log scale): the default heuristic-plus-SMT recognizer scales far past the other paths, with the full-range and permutation backends lower and the exponential reference capped near the bottom. <strong>(B)</strong> Repair over 820 random DAGs: 316 needed repair and all 316 succeeded and verified, 504 were already Wheeler (the spike at 1.0), and there were zero failures. A blow-up below one is node *merging*, not loss — nodes that spell the same string are merged while the represented string set is preserved exactly.
</ZoomFigure>

## What changes for users

The commands are unchanged. The recognizer still runs on a DOT graph with `./bin/recognizer graph.dot` for the default heuristic-plus-SMT path, `-s p` for the permutation backend, and `-f` for the full-range SMT path; the `-f` runs are simply faster now. The generators and the visualizer are untouched. The one genuinely new entry point is repair: pointed at a DAG that the recognizer rejects, the `repair/wheelerize.py` module returns a Wheeler graph that spells the same set of strings, and checks its own work before handing it back.

The main thing I want to carry over from this pass is an ordering of priorities. The most consequential change is not the faster encoding or even the new repair capability — it is that the recognizer's verdicts are now checked against an independent ground truth and a trustworthy second opinion, so a "yes, this is a Wheeler graph" can be relied on. Speed and capability matter, but they come after correctness, because a faster wrong answer helps no one. Building a foundational tool means earning that trust, and then making it quick.

WGT is free and open source. Browse the [code](https://github.com/Kuanhao-Chao/Wheeler_Graph_Toolkit), read the [paper in iScience](https://doi.org/10.1016/j.isci.2023.107402) behind the original toolkit, or see the companion [technical report](/reports/wgt-technical-report/) for the full methods, tables, and reproducibility details. WGT was built with Pei-Wei Chen, Sanjit A. Seshia, and Ben Langmead at Johns Hopkins and UC Berkeley.
